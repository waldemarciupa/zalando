import styled from 'styled-components'

const StyledCard = styled.div`

`

const Card = ({ product }) => {
    return (
        <StyledCard>
            Card
        </StyledCard>
    )
}

export default Card